// Put your own Twitter App keys here. See README.md for more detail.

export const config = {
  consumer_key: process.env.CONSUMER_KEY || 'blah',
  consumer_secret: process.env.CONSUMER_SECRET || 'blah',
  access_token: process.env.ACCESS_TOKEN || 'blah',
  access_token_secret: process.env.ACCESS_TOKEN_SECRET || 'blah',
}
